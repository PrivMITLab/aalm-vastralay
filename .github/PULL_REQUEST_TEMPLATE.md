## Summary of Changes
<!-- Describe the changes made and the motivation behind them. -->

## Checklist
- [ ] Read and followed `.ai/RULES.md`.
- [ ] No `any` TypeScript types introduced.
- [ ] No existing features removed or broken.
- [ ] Database changes are additive-only (`ADD COLUMN IF NOT EXISTS ... DEFAULT ...`).
- [ ] No secrets or sensitive PII exposed in code or logs.
- [ ] `npm test` runs and passes.
- [ ] `npm run typecheck` passes with 0 errors.
- [ ] `npm run lint` passes with 0 warnings.
- [ ] `npm run build` succeeds.
