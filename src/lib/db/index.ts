/**
 * 👑 AALM VASTRALAY — DATABASE ADAPTER RE-EXPORT
 * Provides seamless `@/lib/db` path alias access matching prompt specification,
 * re-exporting pooled db, edge httpDb, and validation utilities.
 */

export * from "@/db";
export { db as default } from "@/db";
