/**
 * 👑 AALM VASTRALAY — FULL SERVER-SIDE AUTH HELPERS
 * Re-exports the full server auth suite from `@/lib/auth` with database access,
 * password verification, and session lifecycle management.
 */

export * from "@/lib/auth";
